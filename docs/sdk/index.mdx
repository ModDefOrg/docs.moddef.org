---
title: SDKs
sidebar_position: 1
slug: /sdk/
---

# SDKs & API reference

ModDef has five implementations that share a conformance suite — decode the same
document to the same values, byte for byte. Each ships idiomatic API docs
generated by that language's native tool, so you land on exactly the reference
your ecosystem expects.

| Language | Package | Focus | API reference |
| --- | --- | --- | --- |
| **Go** | `github.com/ModDefOrg/moddef/go` | reference implementation, CLI | [pkg.go.dev style →](pathname:///sdk/go/) |
| **TypeScript** | `@moddef/core` | Node + browser | [TypeDoc →](pathname:///sdk/typescript/) |
| **Rust** | `moddef-core` | `no_std`, embedded to server | [rustdoc →](pathname:///sdk/rust/) |
| **Python** | `moddef` | asyncio, pymodbus | [Sphinx →](pathname:///sdk/python/) |
| **C** | `moddef-c` | embedded, zero-alloc | [Doxygen →](pathname:///sdk/c/) |

Every runtime provides the same core surface — parse a document, decode/encode
points, resolve measurands, drive a device through a transport — adapted to each
language's conventions. The [Getting started](/guide/getting-started) guide
shows the same read in all five.

:::note
The API reference pages are built by each language's tool and published by CI.
Until the first documentation build runs, the links above show a placeholder.
:::
